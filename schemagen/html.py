class HTMLGenerator():
    def __init__(self, JSONLD, outpath):
        self.JSONLD = JSONLD
        self.outpath = outpath

    def createItemPage():
        # Create pages for each item in a hierarchy
        return False

    def createIndexPage():
        # Create pages for each item in a hierarchy
        return False
